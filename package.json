{
  "name": "tslib-test-utils--monorepo",
  "private": true,
  "scripts": {
    "bump-version": "lerna version --no-git-tag-version --no-push",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
