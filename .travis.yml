branches:
  only:
    - master

language: node_js

node_js:
  - 13

install:
  - npm config set '//npm.pkg.github.com/:_authToken' "${GITHUB_NPM_TOKEN}"

script:
  - make ci

notifications:
  slack:
    rooms:
      - 35up:jwDzQUtpgjOcN0PP8QaUhKZp
    on_success: change
    on_failure: always
